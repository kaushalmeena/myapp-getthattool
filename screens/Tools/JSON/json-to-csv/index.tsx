import React, { FunctionComponent } from "react";
import {
  H3
} from "@blueprintjs/core";

const Home: FunctionComponent = () => (
  <H3>
    Every tool you could want at one place
  </H3>
);

export default Home;
